# 思维群岛

## 当前实现

2026-09-06 新增独立课外训练，面向学生自主尝试。不需要选择教材；原有语文、数学、英语及其学习链路保持独立。

首批四座岛：规律发现、逻辑侦探、空间想象、策略工坊。每岛三条路线，分别标记入门、进阶、挑战；每条四个任务，共 12 条路线、48 个任务。难度标签仅用于内容分组，不代表学生能力等级。

路线方格已改为团子、石头、终点旗和补给背包，角色会逐格移动，可撤回并支持减少动画。没有改变任务或判定规则；实现与追加验证见 [动手练习说明](HANDS_ON_PRACTICE.md)。

入口在首页三科卡片之后、教材地图顶部，也可直接访问 `/thinking-islands`。岛页为 `/thinking-islands/:islandId`；训练页为 `/thinking-islands/:islandId/:missionId`。不存在或不属于该岛的路线显示返回入口，不进入游戏。

## 内容与边界

唯一内容源为 `src/data/thinking/islands.ts`。`src/types/thinking.ts` 定义结构及 `THINKING_ISLANDS_ORIGINAL_V1` 来源：AI 辅助原创、非教材内容、未声明人工审定。不引用现有教材正文，不作为正式课程事实，也不宣称经验证的智力提升效果。

`src/services/thinking/thinkingValidator.ts` 负责确定性判断：

- 选卡：正确选项集合必须完全匹配，不接受重复或未知选项。
- 配对：每人一项、每项一人，同时符合所有结构化线索。
- 排序：完整排列满足题目指定的先后关系，未规定先后的步骤可以交换。
- 路线：上下左右相邻移动，避开障碍，经过所有补给站，最终停在终点且不超过步数限制；不固定唯一走法。
- 组合：每张材料卡最多一次，总长度及指定张数同时满足；不固定唯一组合。

三种难度路线均可直接选择；一条路线内依序完成。两步提示按需打开，答错可修改后重试；没有倒计时、生命消耗或惩罚。小结只显示完成任务数，以及当前页面练习期间发生的重试／提示使用数。

## 进度所有权

`thinkingStore` 与 `thinkingProgressStorage` 拥有本地完成记录。界面只通过该 store 提交方案或查询进度，不修改其他学习领域。

存储键为 `knowledge-island.thinking.v1:<编码后的 profileId>`。`schemaVersion: 1`，记录路线 ID、内容版本、已完成任务 ID；不保存答案、未提交方案、时间、分数、提示次数或重试次数。档案 ID 优先取当前课程档案的 studentId，其次取学生档案，最后使用本机默认档案。

重复完成幂等。刷新恢复到第一个未完成任务；重玩只重置当前页面这一轮，不清除已保存完成记录。保存前合并同一档案已有完成 ID，避免覆盖其他标签页已保存的进度；没有实时跨页通知或云同步。

存储读取使用 JSON 安全解析、Zod 及引用／版本检查。损坏、跨档案、不支持的版本或未知任务记录保留原值，只在内存继续并显示说明；不自动迁移或清除。写入失败保留内存进度，下次答对时重试保存，包括再次完成同一任务。换浏览器或清除浏览器存储不会恢复这些本地记录。

不写入 QuestionAttempt、LearningEvidence、MASTERY_V1、STRATEGY_V1、教材地图进度、LearningHistory、WrongBook 或 Reward。思维群岛的“已完成”不是教材掌握度或正式测评结果。

## 首次接入时的验证

- 新增 `tests/thinking-islands.test.ts`：40 项测试通过，覆盖全部 48 个任务结构与可解性、替代正确方案、错误边界、档案隔离、刷新恢复、幂等、存储损坏／版本不兼容／配额失败、多标签页保存合并和五类交互。
- 首次接入时全量测试 482 项通过；type-check、lint、format:check、build、git diff --check 已通过。
- 浏览器验证：群岛首页、四座岛页、十二条路线页，在 320、768、1280 CSS 像素宽度共 51 组检查，无横向溢出或被测卡片相互重叠。首页与教材地图入口另完成三种宽度的 6 组检查，保留原有三科入口。
- 实际操作完成图形接力站与先想再行动两条路线，覆盖单选、多选、排序、材料组合、路径移动和撤回；另完成逻辑配对任务。提示、错误反馈、刷新续练、路线小结、重玩和回到群岛进度展示已检查。控制台未发现错误。使用独立本地验证地址，未更改用户当前学习页面的档案。

现有构建仍提示主包体积偏大、Zod 注释标记；Node 测试环境存在 localStorage 实验性提示。上述提示不影响本次检查通过，未在此功能内调整依赖或构建拆包。
