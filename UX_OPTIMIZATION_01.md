# 知识岛体验优化 01

2026-09-06。状态：本轮重点改动已实现并验证。本次依据前一轮家长／学生视角体验，优先处理进度丢失、结果误解和开始学习的阻力，不代表所有体验建议都已完成。

## 已实现

### 学过什么，看得明白

- 知识点详情以当前档案、教材和课程的 LessonSession 展示「本课学习进度」，不再沿用入口链接里的旧百分比。完成课程显示 100%；这不是掌握度，也不改变解锁规则。
- 地图节点缺少评估证据时显示「掌握度待评估」，不再出现「已完成」与「还没开始 · 0%」并排。有证据支持的真实零分仍正常显示。
- 首页最近学习带课程名称和中文学科名。没有可用的正式地图统计时，展示实际完成学习次数或开始引导，不伪造 0/0 或零分。

### 闯关可以接着玩

- 教材基础／强化练习和阅读故事练习共用独立的 `questProgressStorage`。通过关卡、当前关卡、提示／错误足迹、错关重练轮、小结状态保存在当前设备。
- 首次进入教材练习默认「基础热身」，孩子可以选择强化。模式和组号分别保存；三组强化练习各自保留进度。
- 进行中的重新闯关需要确认；重新练习不清除累计通过足迹。阅读目录显示累计通过关卡，并提供继续入口。
- 数据带 `schemaVersion: 1`，按档案与练习身份隔离。教材、课程和变式通过练习 ID 隔离，内容修订摘要用于防止旧题的通过状态套用到新题。
- 加载进行 Zod、身份和合法连续进度校验。损坏、未来版本或非法进度保留原始存储，本次可练习但不覆盖；保存失败明确提示。题目变化后旧进度失效，从第一关开始。
- 不保存题目正文、标准答案、未提交答案、未完成的配对／排序草稿。开放表达自查仍只在当前页面保留。

### 提示与开始入口更直接

- 修正分类题沿用数量／单位提示的问题，改为引导孩子先确定分类标准；通用选择题提示改为读清条件、逐项核对。没有宣称所有课程提示均已逐题人工审核。
- 首页提供明确的开始入口；没有正式任务时仍可选一科出发。地图「继续探索」直接进入当前可学习知识点，不再只定位节点并弹提示。
- 仍使用现有可访问性、来源和解锁条件，没有自动生成学习路径或提升内容审核状态。

### 家长先看学习足迹

- 报告优先展示学过的课程、完成课后练习次数、留下完成记录的天数和最近课程名称；显示孩子昵称以及可读的年级／学期。
- 根据当前可读取事实，提供简短陪伴建议，如让孩子讲讲今天的一件发现或一起回看错题。
- 已核验内容的统计与巩固建议默认折叠。没有这些统计不等于孩子没有学习，也不等于全部掌握。
- 新增只读 `participation` 投影。非样本、未被拒绝但仍待核验的内容可以保留参与足迹，并明确标注不作为正式学习结论；正式 History／Mastery／Strategy 的来源筛选没有放宽。
- 足迹按档案、时间和学科隔离，保留旧教材的历史。独立课后闯关和阅读练习仍在各自页面记录，不计入家长报告的正式练习次数。

## 验证记录

- 全量测试：43 个文件、577 项通过。本轮新增覆盖存储版本／损坏／隔离、刷新恢复、提示与重试、重练确认、模式／换组保存、课程结果展示、首页及家长参与足迹。
- `npm run build`（含 type-check）、`npm run lint`、`npm run format:check`、`git diff --check` 通过。Markdown 由项目配置排除在 Prettier 检查外，本文另行核对结构和内容。
- 浏览器测试使用独立来源 `http://127.0.0.1:5174`，不操作用户 `localhost:5173` 的档案、教材选择或学习记录。
- 英语《The Red Bag》通过两关后刷新，恢复第三关；返回阅读目录显示「已通过 2 / 7 关」，再次进入仍可继续。
- 已完成数学《课堂准备 · 倾听与整理》即使通过 `nodeProgress=0` 的旧链接进入，仍显示本课学习进度 100%。地图保留完成状态并显示「掌握度待评估」。家长足迹显示该课程，未将它冒充正式掌握结论。
- 首次基础热身、强化分类提示、首页开始入口、地图直接继续下一课、家长统计折叠／展开已验证。
- 首页、数学地图、数学详情、家长报告、阅读目录、英语故事详情在 320、375、768、1280 CSS 像素宽度共 24 个组合中未发现页面横向溢出；测试标签页未读取到控制台错误。该检查不等于全站所有设备验收。
- 构建仍有既有的大分块与 Zod PURE 注释告警，测试仍有 Node localStorage 实验提示；本次没有解决这些既有工程告警。

## 保持不变与后续边界

保留团子画风、教材内容、课程领域、MASTERY_V1、STRATEGY_V1 及生产来源 guard。独立闯关不写 QuestionAttempt、LearningEvidence、正式 History、WrongBook、Review Queue、奖励或地图完成度。

只在当前设备保存，没有云同步；旧版从未保存的闯关进度无法恢复。中文拼音／朗读、英语录音评分、朗读控制区进一步简化、首次设置流程压缩，以及新的实物操作游戏不在本轮完成范围。

相关说明已同步到 [课后强化训练](QUEST_TRAINING.md)、[阅读群岛](READING_ISLANDS.md) 与 [页面契约](PAGE_SPEC.md)。遵循文档写作规范，区分参与事实、完成状态、正式评估和未实施建议。保留原有工作区修改，未修改 `doc/`，未执行 Git 提交。
