# 本地正式课程与学习流程修复

2026-09-07，项目所有者明确确认：当前本地导入课程为正式课程，直接通过审查。此决定替代此前“正式索引为空、当前导入内容不可发布”的产品限制。审批记录为 `OWNER_LOCAL_RELEASE_2026_09_07`，来源为本次会话的用户指令；不代表外部机构审核，不改写原始来源的版权状态、ISBN 或地区选用证据。

## 已实现

- 正式运行时与本地开发使用同一份批准课程投影，覆盖当前导入的 11 套教材、357 节课程/知识点。原导入数据保留在 `src/data/curriculum/localCatalog.ts`，审批投影、课后测验和发布范围由 `src/data/curriculum/production/localRelease.ts` 维护。SAMPLE 和 golden 开发夹具没有被批准。
- 现有配套闯关中的 1694 道封闭题接入 Assessment；数学看图题保留配图。8 节导读、识字或拼音内容没有封闭测验题，仍可学习，明确列在 `readingOnlyKnowledgePointIds`；自动判分覆盖只计算其余知识点，每个至少有一道有效题。没有编造这些课次的测验完成记录。
- 地图改按学生、数据集、教材保存。原 v1 单教材存储按需迁移并保留备份；切换学科、上下册和教材不清除其他记录。地图可从保留的课程完成会话补回对应节点；没有任何历史凭据的旧丢失数据无法凭空恢复。
- 普通课程新增“重新学习”，整组测验新增“重新挑战”。每次重做使用独立会话，保留旧历史；课程/普通测验重做不重复结算相同基础完成奖励。
- 待巩固通过教材和知识点解析完整课次，直接进入新的巩固测验。已移除学生端“标记完成”按钮；只有匹配的测验全部答对才完成该队列条目。再次出现新作答证据时重新激活，并保留前次完成记录。
- 课程完成的地图回写失败会提示，重新打开已完成课程时幂等重试。读取异常不会静默覆盖损坏的 v2 地图存储。
- 阅读闯关和思维任务写入同一活动记录格式，含学生、内容版本、时间、结果及回访入口。学习历史和本机家长报告增加活动记录；错题页提供闯关中曾答错内容的回顾入口。这些参与记录不冒充教材掌握证据。既有无时间戳记录不会伪造为今天完成。
- 正式构建只显示设备语音；豆包联网合成仍为本机开发工具。旧 question/result 占位路由改为有效页面跳转，未知路由显示可返回首页的 404 页面。报告明确为本机报告，未宣称拥有账号权限隔离。
- 推荐引擎允许没有显式前置关系的教材按现有可访问节点继续学习，保留锁定节点与来源检查，避免正式教材首页推荐为空。今日计划同时接入继续学习建议，并允许曾经生成的空计划补入有效任务。
- 发布脚本实际执行 build、lint、全量测试与生产运行时集成测试；必检项缺失视为失败，NOT_READY 返回非零退出码。GitHub Actions 调用该脚本。

## 本轮边界

正式课程审批已完成；不再要求补充外部教材核验证据才能打开这些本地课程。结构、题目答案与引用完整性仍由工程校验验证。

家庭多孩子账号、全站云同步/统一导出恢复、有效学习时长、后台复习排程、主观题人工批改、发音评分和公网豆包服务仍属于后续功能。当前宠物账号只覆盖宠物备份。阅读/思维新增活动汇总与教材掌握度分开显示；尚未将全部互动题转为教材掌握证据。

## 验证记录

- `npm run release:check`：READY_WITH_LIMITATIONS，0 个阻塞项；包含类型检查、生产构建、lint、54 个文件的 661 项测试，以及生产课程运行时专项测试 2 项，均通过。专项测试逐本检查全部 11 套教材的内容、练习、闯关与学习推荐入口。
- 生产浏览器已走通地区 → 年级 → 语文/数学教材选择 → 角色 → 首页 → 知识点 → 完成课程 → 重新学习；正式测验能够选答、提交并显示判分与解析。
- 浏览器完成语文第一课后切换数学完成第一课，再回到语文，第一课仍为 100%；刷新后首页保留语文 2%、数学 3% 的地图进度，今日任务与推荐正常显示。上下册、其他教材和学生隔离另由存储回归测试覆盖。
- `git diff --check` 通过。生产构建仍有主入口包大小提示，尚未进行低端移动设备性能验收；未部署公网、未执行 GitHub 远端 CI、未逐题人工验收全部教材。
