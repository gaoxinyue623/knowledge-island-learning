# 项目整体检查记录 · 2026-09-07

> 状态说明：以下为修复前的审查快照。用户随后批准当前本地课程作为正式课程，并要求依次修复；最新实现及验证见 [本地正式课程与学习流程修复](LOCAL_RELEASE_FIXES.md)。

本报告面向项目维护者，检查当前工作区的功能覆盖、用户流程和发布准备情况。它记录检查结果与建议，不改变产品范围或既有设计决策。代码和本次执行结果是实现状态的依据；旧阶段文档仅用于对照。

结论：项目已经具备较完整的学习界面和多个本地功能模块，但正式教材入口尚不可用，地图持久化存在跨教材覆盖问题，几条学习活动的数据尚未汇总为统一闭环。下一步应优先修复存储和流程，再扩充内容与功能。

## 1. 检查范围与验证结果

检查了路由与首次配置、教材数据来源、课程播放器、题目会话、阅读闯关、思维训练、错题与复习、奖励与宠物、首页与家长报告、存储、后端和工程脚本。

| 检查 | 本次结果 |
| --- | --- |
| `npm run test:run` | 52 个测试文件、653 个测试通过 |
| `npm run build` | 类型检查和构建通过；主入口 JS 951.27 kB，gzip 317.35 kB，有大包警告 |
| `npm run lint` | 通过 |
| `npm run release:check` | `NOT_READY`，11 条阻塞项；正式教材、单元、课次、知识点、内容、题目均为 0；命令仍以退出码 0 结束 |
| 生产构建浏览器冒烟检查 | 欢迎页 → 开始设置 → 地区页显示“还没有可选地区”，无法继续正常配置 |
| 地图存储最小复现 | 调用真实存储实现，向隔离的内存存储依次保存 math、chinese；math 从 1 条变成 0 条，chinese 为 1 条 |

没有调用付费语音服务，没有部署或注册云端账号。没有逐条审核全部教材题目，没有完成所有页面的真机、无障碍和多浏览器验收；本报告不把单元测试通过等同于这些验收通过。检查开始时已有大量未提交修改，本次只新增此报告，没有修复业务代码。

## 2. 已有模块与实际边界

| 模块 | 已实现 | 当前边界 |
| --- | --- | --- |
| 首次配置与教材选择 | 地区、年级、教材确认、角色与本地档案 | 年级 UI 当前限制一、二年级；生产索引为空 |
| 地图与课程播放器 | 地图节点、前置条件、步骤、恢复、完成记录 | 地图存储仅能保留一套教材；普通已完成课程缺少重新学习会话 |
| 题目与掌握度 | 确定性判题、答题会话、证据、掌握度、学习建议 | 普通整组测验重做不足；开放题不具备完整人工批改闭环 |
| 阅读与思维 | 原创阅读、课后闯关、强化训练、思维任务及本地进度 | 与传统学习历史、错题、报告的数据链路未统一 |
| 错题与复习 | 错题投影、错题重试、巩固队列 | 复习跳转存在断点，手动完成与再次复习机制不足 |
| 奖励与宠物 | 学习奖励、宠物积分、食物、成长、备份恢复 | 宠物账户不等于全站学习账号；学习数据不随宠物恢复 |
| 家长报告 | 教材参与、掌握情况、趋势等只读投影 | 未覆盖全部活动；缺少有效学习时长采集和家长访问隔离 |
| 工程与发布 | TypeScript、lint、653 项测试、生产数据闸门 | 发布命令没有真正阻断流水线，缺少浏览器端到端测试配置 |

## 3. 优先修复的问题

### P1-01：跨教材保存会覆盖地图进度

**已复现。** `learningMapStorage.ts` 用同一个 `knowledge-island.learning-map-progress` 键保存单个 `{ textbookId, records }`。保存语文教材会覆盖先前数学教材的 payload；再次读取数学返回空数组。

影响：跨学科或切换教材后，地图上的已完成状态、解锁状态及依赖这些记录的统计可能丢失。这里确认丢失的是地图存储，不代表所有答题历史都被删除。该存储也没有学生维度，不能直接扩展成多孩子档案。

依据：[地图存储](../../src/services/learning-map/learningMapStorage.ts)，[地图 Store](../../src/stores/learningMapStore.ts)，[课程完成回写](../../src/services/learning-map/learningMapCompletion.ts)。

建议：按学生、教材、数据集保存独立记录，并迁移旧的单教材 payload。验收应覆盖数学 → 语文 → 数学、上下册切换、刷新和不同学生隔离。

### P1-02：正式版本的新用户无法进入教材学习

**代码和生产预览均已验证。** `productionCurriculumData` 的基础选项和教材内容全部为空；生产运行时明确使用该索引。首次配置在地区选择处停止，普通首页和地图又要求先完成配置。

这不是建议放宽审核闸门。需要补齐一个明确的最小发布课程包，或把当前版本定义为可独立进入阅读、思维体验的试用版本，并提供明确入口及范围说明。

依据：[正式课程索引](../../src/data/curriculum/production/index.ts)，[运行时数据选择](../../src/services/runtime.ts)，[生产配置](../../src/config/production.ts)。

### P1-03：待巩固的知识点直达链接参数不完整

**代码确认。** `ReviewQueuePage.openReview()` 在条目没有 `mapNodeId` 时，仅传递 `textbookId`；目标详情页要求 query 同时提供 `textbookId`、`unitId`、`lessonId`、`knowledgePointId`，否则上下文为 null。路径里的知识点 ID 不会自动补齐该 query。

影响：这类条目点击“去巩固”无法加载正常详情。存在地图节点的分支也只传 `focusNodeId`，没有传教材，需要同时验收旧教材复习条目能否打开正确地图。

依据：[复习入口](../../src/pages/ReviewQueuePage.vue)，[详情页上下文校验](../../src/pages/KnowledgePointDetailPage.vue)。

建议：通过统一导航解析函数，从教材与知识点解析完整课次上下文；找不到关联时给出可恢复的提示。验收无地图节点、跨教材、内容已移除三个场景。

### P1-04：正常课程与整组测验缺少重做流程

**代码确认，限定为普通 LessonPlayer / Assessment 路径。** 已完成会话会被恢复成 `completed`；课程导航和答题操作拒绝继续，重置接口和按钮只对 demo / dev 开放。知识点详情却提供“再次进入完整学习”。

影响：再次进入通常回到旧结果，无法形成新一轮独立作答与学习记录。错题专用重试有 `sessionScope`，阅读闯关也有自己的重练机制，不能据此认为普通整组测验已经支持重做。

依据：[课程 Store](../../src/stores/lessonPlayerStore.ts)，[答题 Store](../../src/stores/questionEngineStore.ts)，[课程完成页](../../src/pages/LessonPlayerPage.vue)，[测验页](../../src/pages/QuestionEnginePage.vue)。

建议：提供明确的“回看内容”和“重新挑战”；新挑战生成 attempt/session 标识，保留历史，奖励继续按规则去重。

### P1-05：复习没有以作答结果闭环，已完成条目也不会自动重新激活

**代码确认。** 队列页允许直接“标记完成”，Store 随即尝试产生 `review_completed` 奖励，不要求关联练习证据。随后同一教材、知识点、节点和建议类型再次生成建议时，ID 不变；repository 的 `upsert()` 无条件保留旧 `completed` 状态。普通列表默认隐藏已完成条目。

影响：未练习也可以被统计为完成；相同薄弱点后续再次需要巩固时可能仍隐藏在已完成列表，除非用户手动重新加入。当前队列也没有完整到期时间、复习间隔和延期机制。

依据：[队列 Store](../../src/stores/reviewQueueStore.ts)，[队列 ID 与 upsert](../../src/services/review-queue/reviewQueueRepository.ts)，[队列页面](../../src/pages/ReviewQueuePage.vue)。

建议：区分“手动处理”和“练习达标”；携带 reviewItemId 进入训练，按结果完成或保留。新的学习证据应能够开启新的复习轮次，不覆盖已完成历史。

### P1-06：新增学习活动有进度和积分，但未进入完整学习统计

**调用链确认。** `ReadingQuest` 保存独立 quest 进度并结算宠物奖励；`thinkingStore` 保存独立思维进度并通知宠物模块。这些路径没有写入传统 LearningHistory / WrongBook / Mastery 数据。家长报告的参与汇总仅消费 `lesson_completed` 和 `assessment_completed`。

影响：孩子完成阅读或思维任务、获得积分，但教材学习历史、错题本与报告不会相应展示这些活动。Quest 错误只留在该组练习的 mistakeIds，不能进入全局错题复习。思维、Quest 进度也没有足够的会话时间信息用于按天还原完整活动。

依据：[ReadingQuest](../../src/components/knowledge-point/ReadingQuest.vue)，[Quest 存储](../../src/services/content-expansion/questProgressStorage.ts)，[思维 Store](../../src/stores/thinkingStore.ts)，[宠物参与奖励](../../src/services/pet/petQuestRewards.ts)，[报告数据来源](../../src/services/parent-report/parentReportService.ts)。

建议：建立统一学习活动记录，至少包含学生、内容版本、活动类型、会话、时间和结果。参与事实先进入历史和报告；只有满足证据要求的活动才更新教材掌握度，不能把宠物积分直接当作掌握证据。

### P1-07：发布检查报告失败，但不会阻止发布命令继续执行

**退出码已验证。** `release:check` 输出 `NOT_READY` 仍返回 0；脚本仅打印报告。此外传入的 engineering、qa、regression 均为空对象，门禁只遍历已有键，没有把缺少必检项视为失败。`build` 没有串联 release 检查。

影响：若未来流水线用 `release:check && deploy`，当前检查不会阻止后续命令；即使内容条件将来通过，缺失工程或 QA 结果也不会自动构成失败。

依据：[发布脚本](../../scripts/mvp-release.ts)，[门禁实现](../../src/services/production-readiness/mvpReleaseGate.ts)，[npm scripts](../../package.json)。

建议：NOT_READY 返回非零退出码，定义必检项并校验缺失值，把实际测试与浏览器验收结果接入发布流程。

### P2-01：豆包语音选项在生产页面存在，但服务仅挂载在开发服务器

**代码确认，未调用实际语音合成。** 下拉框始终提供豆包选项；前端请求 `/api/tts/doubao`。实现只通过 Vite `configureServer` 挂载，而且仅接受本机 HTTP 来源。生产构建不含这段服务，宠物后端也没有此路由。

建议：若保留本地工具定位，生产隐藏此选项；若作为产品能力，补充正式接口和服务可用性探测，以及配额与失败回退。仅部署 dist 不会获得豆包合成能力。

依据：[声音选项](../../src/components/lesson-player/SpeechProviderSelect.vue)，[语音调用](../../src/composables/useEnglishReadAloud.ts)，[本地插件](../../scripts/tts/vitePlugin.ts)，[服务入口](../../server/index.ts)。

### P2-02：课程完成回写缺少完整失败补偿

**代码确认。** `completeLesson()` 先设置 completed、保存会话、投影历史与奖励，再等待地图回写。地图不存在时返回 false 未被检查，存储异常可抛出；重新载入完成会话只补投影历史和奖励，没有重试地图完成回写。

影响：在地图不可读或写入异常时，可出现课程显示完成而地图未更新的状态；这与跨教材覆盖是不同的失败路径。

依据：[课程完成和载入](../../src/stores/lessonPlayerStore.ts)，[地图完成服务](../../src/services/learning-map/learningMapCompletion.ts)。建议统一记录待处理结果，按幂等方式重试各投影，并保留可见的错误反馈。

### P2-03：路由与用户角色还未形成完整边界

**代码确认。** `parentOnly` 和 `studentOnly` 是路由元数据，守卫没有执行身份检查；缺少全局未知路由页；`/question/:nodeId`、`/result/:nodeId` 仍是占位页。`/tasks` 复用首页，尚无独立任务管理能力。

家长报告当前是本机只读页面，因此这里是产品边界缺失，不能直接描述成已存在的跨账号数据泄漏。若要支持家庭账号、多孩子和远程家长报告，需要建立真实授权。

依据：[路由](../../src/router/index.ts)，[路由守卫](../../src/router/guard.ts)。建议移除或重定向旧占位路径、增加 404，明确“本机学习报告”和“受保护的家长中心”的产品差异。

## 4. 缺失或需要补齐的模块

以下是能力缺口或建议，不全部属于当前 MVP 必须实现的缺陷。

| 模块 | 当前缺口 | 建议顺序 |
| --- | --- | --- |
| 最小可发布课程包 | 候选内容与审核脚本已有，但没有进入正式索引的课程包 | 上线教材学习前必须补齐 |
| 学习活动与会话统一层 | 阅读、思维、普通课程和测验记录分散；缺少统一重做与复习轮次 | 优先 |
| 全站数据备份与恢复 | 目前只备份宠物；教材进度、设置、错题、会话没有统一可携带备份 | 优先，至少先做本地导出恢复 |
| 有效学习时长 | 没有可用于报告的前台有效时长、暂停和闲置处理；开始结束时间不能直接当作实际学习时长 | 家长报告下一阶段 |
| 家庭账号与多孩子 | 本地单档案为主，宠物账号没有扩展为全站学生身份；地图还缺档案隔离 | 有多设备/家庭需求时 |
| 账号生命周期 | 宠物账号已有注册登录退出，但找回密码、改密和账号删除页面缺失 | 公网提供长期账号前明确方案 |
| 复习排程与学习计划调整 | 已有规则建议与今日任务，但不是完整的到期复习、延期、难度调整和自适应路径 | 基础闭环修好后 |
| 开放题批改与口语评价 | 主观题缺审核队列与结果回流；英语朗读是播放辅助，没有录音识别和发音评价 | 根据课程需求决定 |
| 生产运行与故障排查 | 缺可验证的发布流水线、浏览器 E2E 配置、集中错误采集及完整生产联调证据 | 发布前 |

全站备份和账号边界依据：[宠物后端](../../server/petServer.ts)、[云端备份面板](../../src/components/pet/PetCloudPanel.vue)、[设置](../../src/pages/SettingsPage.vue)、[PET_BACKEND](PET_BACKEND.md)。测试范围依据：[Vitest 配置](../../vitest.config.ts)和[脚本](../../package.json)。

## 5. 建议实施顺序与验收

1. **先保住进度**：修复跨教材覆盖，增加旧存储迁移与异常回写补偿。验收跨三科、上下册、刷新、存储异常。
2. **再打通复习**：修复直达参数，支持普通课程回看和测验重做，复习完成关联结果，新错误可重新激活巩固任务。
3. **统一活动记录**：阅读和思维完成进入历史及家长报告；参与奖励与教材掌握分开计算；明确有效时长口径。
4. **明确可交付版本**：若交付教材同步产品，先完成最小审核课程包；若交付阅读/思维试用版，入口和文案应与真实开放能力一致。该范围选择仍待项目方决策。
5. **建立真正发布门禁**：失败退出码、必检项、生产构建浏览器用例、语音/API 部署边界和备份恢复验收。

优先增加的浏览器流程：新用户首次配置；完成数学后完成语文再返回数学；普通测验答错后重做；巩固条目直达与再次激活；阅读/思维完成后查看历史与家长报告；刷新恢复；生产语音不可用时的反馈。

## 6. 文档状态不一致

`PRODUCT.md` 仍写首个 MVP 聚焦三年级上册、没有账号和云同步；当前年级 UI 限制一二年级，且宠物已有账号备份后端。`MVP Scope` 仍围绕旧候选范围。这些来源反映不同阶段，不应据旧文档断言当前完全没有后端，也不能据宠物后端宣称全站已经云同步。

本报告保留上述冲突，不替项目方重新决定产品范围，也未改写旧事实源。建议确认当前交付对象后再统一更新产品范围、发布清单和用户说明。首包体积优化可随后进行；本次只确认构建体积告警，没有测得真机首屏耗时，不能据此给出性能时延结论。
