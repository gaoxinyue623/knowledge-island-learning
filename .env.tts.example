# 火山引擎语音合成配置模板，可提交；不要在此文件填写真实密钥。
# 将配置填入同目录的 .env.tts.local；该本地文件必须保持 Git 忽略。
# 仅供本地 Node 合成服务与脚本读取，禁止导入前端或改为 VITE_ 前缀。
# 本机 npm run dev 的语音接口和 npm run tts:generate 读取本地配置。
# 默认浏览器朗读不调用豆包；选豆包后点击朗读或明确运行生成命令才可能计费。

# 豆包语音服务的 API Key，不是普通 AK/SK 或其他产品的密钥。
VOLC_TTS_API_KEY=""

# 所选语音合成模型的 Resource ID，必须与开通的服务、音色匹配。
VOLC_TTS_RESOURCE_ID=""

# 支持英语的音色 ID（speaker），填写控制台 ID，不是音色显示名称。
VOLC_TTS_SPEAKER=""
