# 知识岛｜多端响应式设计

> 本文档定义 Desktop、Tablet Landscape、Tablet Portrait 和 Mobile 的布局、导航、触控、键盘、地图、LessonPlayer、题目与首次课程配置响应式规则。PHASE 7 LearningMap 与 PHASE 8 LessonPlayer 已完成浏览器视口验证；其他页面仍以设计规格为准。

## 文档状态

| 项目 | 内容 |
| --- | --- |
| 所属阶段 | PHASE 8.4：LessonPlayer / Knowledge Learning Flow（继承 PHASE 3 响应式设计） |
| 状态 | LearningMap 与 LessonPlayer 已完成并验证；Question、其他配置页面仍为设计规格 |
| 上游事实源 | `PRODUCT.md`、`DESIGN_SYSTEM.md`、`PAGE_SPEC.md`、`UI_FLOW.md` |
| 设备范围 | Desktop、Tablet Landscape、Tablet Portrait、Mobile |
| 目标 | 同一学习任务在不同设备保持信息优先级，不是把桌面页面整体缩小 |

---

## 1. 设备分层

| 设备 | 视口范围 | 核心使用方式 | 布局原则 |
| --- | --- | --- | --- |
| Desktop | `≥1200px` | 鼠标、键盘、大场景 | 可使用双栏、多栏和大地图；普通内容最大约 1440～1600px |
| Tablet Landscape | `768–1199px` 横屏 | 触控 + 键盘 | 利用横向空间展示场景或双栏，控件保留触控尺寸 |
| Tablet Portrait | `768–1199px` 竖屏 | 触控 | 更接近移动单列，但可保留两列卡片或上下分区 |
| Mobile | `<768px` | 单手触控 | 一步一屏、单列、底部导航；普通内容不横向滚动 |

同时使用 Container Query 思想：组件由自身可用空间决定内部布局，不能把所有响应式差异都写成全局 window width 分支。

## 2. 全局布局 Token

| 项目 | Desktop | Tablet | Mobile |
| --- | --- | --- | --- |
| 页面水平边距 | 32～64px | 24～40px | 16～20px |
| 普通内容最大宽度 | 1440～1600px | 100% 减安全边距 | 100% 减安全边距 |
| Onboarding 最大宽度 | 900～1100px | 680～900px | 单列全宽 |
| Question 阅读宽度 | 760～920px | 680～820px | 100% 减 32～40px |
| 卡片列间距 | 20～24px | 16～20px | 12～16px |
| 默认触控目标 | ≥44px | ≥44px | ≥44px，主选项优先 48px |
| Bottom Navigation | 不默认使用 | 可选 | 固定底部，配合 safe area |

固定宽度、全页面横向滚动和通过缩小字体解决拥挤均不符合本设计。

## 3. 导航策略

### 3.1 Desktop

使用轻量游戏化 Dock 或 Compact Sidebar，包含：

```text
首页 / 地图 / 任务 / 错题本 / 我的
```

顶部保留 `StudentHeader`。导航有图标和短文案，不采用后台管理系统的密集树形菜单。家长中心从账户区域进入。

### 3.2 Tablet

- Landscape：可使用窄侧栏或顶部紧凑导航，保留大场景宽度。
- Portrait：可采用顶部返回 + 页面标题，底部或抽屉导航；防止导航遮挡主任务。
- 导航与主 CTA 至少保留 44×44px 触控目标。

### 3.3 Mobile

底部导航固定为：

```text
首页  /  地图  /  任务  /  错题本  /  我的
```

每项使用 `AppIcon + Text`，不使用 Emoji；底部导航支持 `safe-area-inset-bottom`，不能遮挡最后一个按钮或输入框。

## 4. 首页响应式

首页优先级保持：继续学习 > 今日任务 > 三个学科世界 > XP / 星星 / 金币。首页默认只显示“年级 · 学期”，地区和三科教材放在“我的学习设置”。

### 4.1 Desktop Home Wireframe

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│  Knowledge Island Dock  │ StudentHeader：头像  小明  三年级·上册  资源        │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│                  [知识团子 + 知识岛场景插画]                                │
│                                                                              │
│       ┌────────────────────────────┐   ┌────────────────────────────────┐   │
│       │ 继续学习                    │   │ 今日任务                        │   │
│       │ 数学 · 当前关卡             │   │ 4 / 6    约 12 分钟             │   │
│       │ [继续学习]                  │   │ 语文  数学  英语  复习          │   │
│       └────────────────────────────┘   └────────────────────────────────┘   │
│                                                                              │
│       ┌───────────────┐   ┌───────────────┐   ┌───────────────┐              │
│       │ 语文世界       │   │ 数学世界       │   │ 英语世界       │              │
│       │ 进度 / 当前单元│   │ 进度 / 当前单元│   │ 进度 / 当前单元│              │
│       └───────────────┘   └───────────────┘   └───────────────┘              │
└──────────────────────────────────────────────────────────────────────────────┘
```

Desktop 可以把今日任务放在主任务旁边，但不得用右侧表格或后台卡片墙取代主场景。

### 4.2 Tablet Landscape Home Wireframe

```text
┌──────────────────────────────────────────────────────────────┐
│ 轻量 Dock │ 小明  三年级·上册                         [头像] │
├──────────────────────────────┬───────────────────────────────┤
│                              │  今日任务                      │
│     知识岛场景 + 团子         │  4 / 6 · 约 12 分钟             │
│                              │  [开始今日任务]                 │
│   ┌──────────────────────┐   ├───────────────────────────────┤
│   │ 继续学习              │   │ 语文世界 │ 数学世界 │ 英语世界  │
│   │ [继续]                │   │ 进度摘要 │ 进度摘要 │ 进度摘要  │
│   └──────────────────────┘   └───────────────────────────────┘
└──────────────────────────────────────────────────────────────┘
```

### 4.3 Tablet Portrait Home Wireframe

```text
┌───────────────────────────────┐
│ 头像  小明  三年级·上册         │
├───────────────────────────────┤
│       小场景 + 知识团子         │
├───────────────────────────────┤
│ 继续学习                       │
│ 数学 · 当前关卡       [继续]    │
├───────────────┬───────────────┤
│ 今日任务 4/6   │ 约 12 分钟     │
├───────────────┴───────────────┤
│ 语文世界          数学世界      │
│ 英语世界          [查看地图]    │
├───────────────────────────────┤
│ 首页  地图  任务  错题本  我的   │
└───────────────────────────────┘
```

### 4.4 Mobile Home Wireframe

```text
┌──────────────────────┐
│ 头像  小明  三年级·上册│
├──────────────────────┤
│ 继续学习              │
│ 数学 · 当前关卡       │
│ [     继续学习      ] │
├──────────────────────┤
│ 今日任务  4 / 6       │
│ 预计剩余约 12 分钟    │
├──────────────────────┤
│ 语文世界              │
│ 数学世界              │
│ 英语世界              │
├──────────────────────┤
│ 首页 地图 任务 错题本 我的│
└──────────────────────┘
```

Mobile 不展示地区、三科出版社或教材年份；点击年级 / 学期标签进入“我的学习设置”。

## 5. Onboarding 响应式

### Desktop

- 居中主卡片，最大宽度约 900～1100px。
- RegionSelect 可双栏：左侧省份 / 推荐地区，右侧城市或结果；仅当数据实际支持城市时显示城市列。
- 顶部轻量进度显示“地区 · 年级 · 教材 · 角色”，如 `1 / 4`。

### Tablet

- 大卡片和 44～48px 触控目标。
- Landscape 使用双栏，Portrait 可使用单列 + 可横向查看的区域卡片，但不把桌面表单缩小。

### Mobile

- 一步一屏、单列，返回上一页保留已选草稿。
- RegionSelect 采用省 → 城市逐级选择；如果 MVP 只有省级，就不展示城市入口。
- 三科教材卡片纵向排列；版本选择使用 BottomSheet 或全屏选择页，保证单手完成。

## 6. Learning Map 响应式

### Desktop LearningMap

```text
┌──────────────────────────────────────────────────────────────────┐
│ 返回  数学世界 / 计算工厂                    三年级·上册 · 教材   │
├──────────────────────────────────────────────────────────────────┤
│  [缩放 -]       START ── LESSON ── PRACTICE ── CHALLENGE ── BOSS  │
│                         (节点)  [当前角色]     (节点)   [锁定]    │
│  [缩放 +]                                                     │
│  主题说明 / 图例 / 当前节点提示                                   │
└──────────────────────────────────────────────────────────────────┘
```

支持鼠标拖动、滚轮平移和缩放；地图可以接近全宽，但题目和说明仍控制阅读宽度。

### Tablet LearningMap

```text
┌──────────────────────────────────────────┐
│ 返回  数学世界      三年级·上册           │
├──────────────────────────────────────────┤
│         (节点)──(当前)──(节点)──[锁定]     │
│             知识团子                     │
│  [拖动地图]              [缩放]           │
├──────────────────────────────────────────┤
│ 当前关卡：基础练习       [继续]           │
└──────────────────────────────────────────┘
```

触控拖动和缩放必须可用，当前节点信息固定在可读区域，不随地图拖走。

### Mobile LearningMap

```text
┌──────────────────────┐
│ 数学世界              │
│ 计算工厂              │
│ 三年级·上册 · 教材     │
├──────────────────────┤
│          [当前节点]   │
│          │            │
│          (节点)       │
│          │            │
│          [锁定]       │
│  [当前关卡说明]       │
│  [继续学习]           │
├──────────────────────┤
│ 首页 地图 任务 错题本 我的│
└──────────────────────┘
```

Mobile 使用纵向路径，当前节点优先居中；不要求双指操作才能找到当前关卡。地图可进行受控特殊滚动，但普通内容页不横向滚动。

## 7. Lesson 与 Question 响应式

### Lesson

- Desktop：中央 LessonPlayer + 侧边小型步骤 / 进度；一个屏幕一个学习重点。
- Tablet Landscape：内容与角色 / 媒体双栏；Portrait 改为上下排列。
- Mobile：返回、关卡标题和进度固定在顶部；中央内容单列，底部主按钮避开安全区。

### Question

- Desktop：题干、媒体和交互区控制在 `layout.questionMax`，辅助说明可在侧边或题干下方。
- Tablet：优先一列大选项；Landscape 可用左右分区，Portrait 用上下分区。
- Mobile：题干、媒体、交互和提交按单列顺序排列；软键盘出现时滚动到当前输入，不遮挡提交按钮。
- 所有题型共用 `QuestionShell`，不同题型只改变交互区域。

具体题型布局、键盘操作和 Wireframe 见 `QUESTION_UI.md`。

## 8. Safe Area、触控与键盘

- 支持 `safe-area-inset-top` 与 `safe-area-inset-bottom`。
- Mobile Bottom Navigation、固定提交栏和 BottomSheet 不遮挡内容。
- 可拖拽题提供点击项目 → 点击目标的替代操作；匹配题提供点击 A → 点击 B；排序题提供上移 / 下移。
- 输入题在软键盘出现时保持当前空位可见；不要依赖用户手动缩放页面。
- Tablet 外接键盘时 Focus 顺序与触控顺序一致。

## 9. Reduced Motion 与性能

- 支持 `prefers-reduced-motion`，关闭漂浮、循环装饰、大粒子和地图自动镜头。
- 保留必要的状态切换、Focus、选中和加载反馈；必要时改为静态图标与文案。
- Mobile / Tablet 上限制同时运行的角色、地图和奖励动画数量；离屏场景暂停动画。
- 任何奖励动效约 1～2 秒且可跳过，不能阻塞继续按钮。

## 10. 响应式验收

1. Desktop、Tablet Landscape、Tablet Portrait、Mobile 有明确布局差异。
2. Mobile 不是 Desktop 缩小版；普通页面不横向滚动。
3. Onboarding、教材卡片、题目选项和底部导航可单手触控。
4. 地图在不同设备仍能表达当前位置、下一节点和锁定原因。
5. 软键盘、Safe Area、键盘 Focus 和 Reduced Motion 均有设计位置。
6. Tablet 横屏和竖屏都充分利用空间，不只按比例缩小。

## 11. PHASE 7 LearningMap 工程验证

LearningMap 使用同一份逻辑坐标 ViewModel，在不同视口通过容器缩放和断点样式呈现：移动端保持纵向滚动和底部详情面板，Tablet 保留大地图区域，Desktop 使用更宽的场景与侧边详情面板。浏览器已验证 `375×812`、`390×844`、`430×932`、`768×1024`、`1024×768` 和 `1440×900`，均无横向溢出；节点数、路径和文本在各视口保持可见。

该验证覆盖地图壳与 LessonPlayer 的基础布局；不表示 Question、教材配置或正式课程数据已经发布。LessonPlayer 还验证了步骤导航、媒体回退、按钮可用性和状态提示。Reduced Motion 规则由全局样式、地图样式和 LessonPlayer 样式共同落实，装饰不承担交互语义。
