# 知识岛｜首次课程配置体验设计

> 本文档专门描述首次使用时的地区、年级、教材和角色配置体验。它是 PHASE 3 的设计规格，不是注册表单、后端流程或页面实现。

## 文档状态

| 项目 | 内容 |
| --- | --- |
| 所属阶段 | PHASE 3：UI / UX + 游戏视觉系统 + 多端响应式设计 |
| 状态 | 仅设计；未实现 |
| 上游事实源 | `PRODUCT.md`、`CURRICULUM.md`、`DATA_MODEL.md`、`CONTENT_REVIEW.md` |
| 关联文档 | `PAGE_SPEC.md`、`UI_FLOW.md`、`RESPONSIVE_DESIGN.md`、`CHARACTER_DESIGN.md` |
| 目标 | 让首次用户完成地区 → 年级 → 教材 → 角色，并进入知识岛 |

---

## 1. 体验目标

首次进入不是设置向导，而是进入知识岛之前的一段轻量游戏开场：

```text
欢迎来到知识岛
  ↓
你在哪里学习？
  ↓
你现在几年级？
  ↓
确认一下你的课本
  ↓
选择你的知识团子
  ↓
出发去知识岛
```

体验承诺：

- 每一步只解决一个问题。
- 每一步只有一个主要 CTA，允许返回上一页。
- 已选地区、年级和教材以草稿保存，返回时不丢失。
- 地区、出版社、教材版本永远是三个不同概念。
- UI 只展示课程数据层的结果，不依据地区名称猜教材。
- 完成配置后再次进入应用直接到 Home，不重复填写。

## 2. 进度与视觉节奏

顶部使用轻量进度而不是复杂 Stepper：

```text
1 / 4    地区 · 年级 · 教材 · 角色
```

页面只在当前步骤突出显示，已完成步骤可以回看；未来步骤不显示复杂必填清单。进度状态使用 `AppIcon`、短标签和位置变化表达，不使用 Emoji。

| 步骤 | 页面 | 核心问题 | 主 CTA |
| --- | --- | --- | --- |
| 1 | `RegionSelect` | 你在哪里学习？ | 下一步 |
| 2 | `GradeSelect` | 你现在几年级？ | 下一步 |
| 3 | `TextbookConfirm` | 你使用哪套课本？ | 确认并继续 |
| 4 | `CharacterSetup` | 选择你的知识团子 | 出发去知识岛 |

`WelcomeOnboarding` 是进度前的开场入口；`OnboardingComplete` 是保存前的轻量回顾。

## 3. WelcomeOnboarding

### 3.1 内容与布局

- 中央显示知识团子和抽象知识岛原创占位插画。
- 主文案：“欢迎来到知识岛”。
- 辅助文案：“先告诉我你的学习地区和年级，我来帮你准备课本。”
- 进度提示“地区 · 年级 · 教材 · 角色”。
- 主 CTA：“开始设置”。
- 次 CTA：“请家长帮我设置”。

不出现“注册表单”“课程系统参数”“选择出版社”等后台词汇。没有真实角色资产时使用 `KnowledgeDangoPlaceholder` 或基础几何原创占位。

### 3.2 返回与恢复

若存在未完成草稿，主 CTA 改为“继续上次设置”，并提供“重新开始设置”；重新开始只清除未确认草稿，不删除已保存学习记录。

## 4. RegionSelect：地区选择

### 4.1 核心交互

页面文案固定为“你在哪里学习？”；字段语义是 `regionId`。推荐内容：

1. 搜索框。
2. 最近 / 推荐地区（如果产品已有此数据）。
3. 省份列表。
4. 城市列表（只有真实 `CITY` 数据存在时才显示）。

Desktop 可以省份 / 城市双栏；Tablet 使用大卡片；Mobile 使用逐级页面：

```text
省份列表 → 选择广东省 → 城市列表 → 选择深圳市 → 确认
```

如果 MVP 只支持省级，直接完成省级选择，不提前显示城市入口或模拟城市能力。默认不使用省 / 市 / 区三级 Cascader。

### 4.2 地区状态与恢复

| 状态 | 学生端文案 | 操作 |
| --- | --- | --- |
| `NORMAL` | “选择一个你所在的地区” | 选择卡片 |
| `SEARCHING` | “正在找地区” | 等待或取消搜索 |
| `NO_RESULT` | “没有找到这个地区，换个关键词试试” | 修改关键词 |
| `REGION_NOT_SUPPORTED` | “这个地区的课程还在准备中” | 选择其他地区 |
| `LOADING` | “正在准备地区列表” | 等待 / 重试 |
| `ERROR` | “地区列表没有加载出来” | 再试一次 |
| `OFFLINE` | “网络暂时不可用” | 使用可用缓存或重新连接 |

禁止 `404`、`No Data`、`Null`、`Undefined`。地区状态不因为某出版社存在而自动变为已支持。

### 4.3 地区与出版社的视觉区分

- 地区卡片使用 `MapPin`、`Map` 或 `Navigation` 图标。
- 地区卡片只显示地区名称与层级，不显示出版社名称作为地区标签。
- 教材卡片才显示出版社；出版社使用 `BookOpen`、`Library` 或 `BookMarked` 图标。

## 5. GradeSelect：年级选择

### 5.1 核心交互

显示一年级至六年级六张大型 `GradeCard`：

```text
[ 1  一年级  即将开放 ]  [ 2  二年级  即将开放 ]
[ 3  三年级  当前支持 ]  [ 4  四年级  即将开放 ]
[ 5  五年级  即将开放 ]  [ 6  六年级  即将开放 ]
```

MVP 只允许三年级进入下一步；其他年级显示“即将开放”，卡片不可进入空课程。未开放卡片说明原因并保持可访问的禁用语义。主交互不是 Select Dropdown。

### 5.2 响应式

- Desktop：2～3 列大卡片，数字与年级名称并列突出。
- Tablet：卡片间距和高度优先触控，不压缩成表格。
- Mobile：单列或两列大卡片，保证 44×44px 以上点击区域。

## 6. Resolve Available Textbooks

### 6.1 调用边界

完成地区、年级和学期选择后，进入解析状态：

```text
regionId + gradeId + semesterId
  ↓
resolveAvailableTextbooks
  ↓
chinese[] / math[] / english[]
```

UI 不负责查询关系、判断出版社、补全教材名称或猜测默认版本。解析器只读取有效且已核验的 `Region`、`RegionTextbookRelation`、`TextbookVersion` 和 `Publisher`。

### 6.2 各学科独立分支

| 解析结果 | UI 表达 | 下一步 |
| --- | --- | --- |
| 唯一有效 `DEFAULT` | “系统推荐” + 教材卡 | 用户确认 |
| 多个 `SUPPORTED` / `OPTIONAL` | “这里有几种课本版本，请确认你正在使用哪一本” | 更换版本 / 手动确认 |
| 没有唯一 `DEFAULT` | `NO_DEFAULT_TEXTBOOK` + 查看课本提示 | 手动选择 |
| 没有候选 | `TEXTBOOK_NOT_FOUND` | 重试、返回或手动选择 |
| 关系待核验 | `TEXTBOOK_NEEDS_CONFIRMATION` | 不当作确定事实；等待确认 |

语文、数学、英语分别执行分支。数学没有默认版本不能影响语文卡片显示，也不能用语文选择推断英语版本。

## 7. TextbookConfirm：教材确认

### 7.1 视觉布局

页面主文案：“确认一下你的课本”。顶部摘要只显示已选择的地区、年级和学期。主体是三张 `TextbookCard`：

```text
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ BookOpen     │  │ BookOpen     │  │ BookOpen     │
│ 语文         │  │ 数学         │  │ 英语         │
│ 出版社：…    │  │ 出版社：…    │  │ 出版社：…    │
│ 版本：…      │  │ 版本：…      │  │ 版本：…      │
│ 三年级·上册  │  │ 三年级·上册  │  │ 三年级·上册  │
│ [更换版本]   │  │ [更换版本]   │  │ [更换版本]   │
└──────────────┘  └──────────────┘  └──────────────┘
```

完整名称、出版社、年级、学期、版次 / 年份（有数据时）和推荐状态均来自数据层。UI 不能自己拼教材名称。

### 7.2 多版本选择

点击某学科“更换版本”进入该学科的 `TextbookVersionSelector`。选择器显示：

- 教材完整名称。
- 出版社完整名称或显示名。
- 适用范围。
- 版次 / 年份（有数据时）。
- 版本说明。
- 是否推荐及推荐依据。

选择器只接收该学科候选集，不加载或覆盖其他学科。

### 7.3 无法确定教材

当某学科没有默认版本或存在多个候选：

- 显示“这里有几种课本版本，请确认你正在使用哪一本”。
- 显示 `看看我的教材版本？` 入口。
- 允许“请家长帮我确认”。
- 不随机选择、不默认猜测、不调用 AI 推断出版社。

帮助入口打开 BottomSheet / Modal，说明可以从课本封面查看出版社、年级、上 / 下册和教材名称。MVP 不实现 OCR，只保留入口和布局。

### 7.4 教材确认保存

每张卡的确认状态独立保存到草稿；只有语文、数学、英语三科都有版本时，主 CTA“确认并继续”才可用。确认后形成：

```text
StudentCurriculumProfile
├─ regionId
├─ gradeId
├─ semesterId
├─ chineseTextbookVersionId
├─ mathTextbookVersionId
└─ englishTextbookVersionId
```

## 8. CharacterSetup

教材确认后切换语气：“选择你的知识团子”。

- 角色预览优先，装扮选择少量、低认知负担。
- 主 CTA：“出发去知识岛”。
- 次 CTA：“稍后再选”。
- 装扮只影响外观，不影响答案、难度、解锁和公平性。
- 视觉状态遵守 `CHARACTER_DESIGN.md`，错误不使用失败倒地或哭泣。

## 9. OnboardingComplete

进入首页前做一次轻量复核：

- 所在地区。
- 三年级 · 上册或数据层返回的当前年级 / 学期。
- 语文、数学、英语教材摘要。
- 知识团子外观。

主 CTA：“进入知识岛”。点击前保存 `StudentCurriculumProfile.confirmedAt`；保存失败时显示“还差一步，重新保存”，保留草稿，不进入缺少课程上下文的 Home。

## 10. 保存与返回策略

| 时点 | 保存内容 | 是否成为已确认 profile |
| --- | --- | --- |
| 选择地区 | 临时 `regionId` | 否 |
| 选择年级 / 学期 | 临时课程范围 | 否 |
| 解析教材 | 候选集和推荐依据 | 否 |
| 选择某科版本 | 对应科草稿字段 | 否 |
| 三科确认 | 三科完整选择 | 待完成页保存 |
| OnboardingComplete 成功 | `confirmedAt` + 三科字段 | 是 |

返回上一页不得清空已选数据。异常、离线或关闭应用后再次进入时恢复草稿；用户取消“重新开始”时旧的已确认档案不受影响。

## 11. 响应式体验

### Desktop

- Onboarding 主卡片最大约 900～1100px，居中展示。
- RegionSelect 可双栏省份 / 城市。
- TextbookConfirm 三卡并列或两列布局。

### Tablet

- Landscape 维持双栏探索感；Portrait 使用大卡片和单列分区。
- 触控目标至少 44×44px，教材卡信息层级不压缩。

### Mobile

- 一步一屏、单列、顶部轻量进度。
- 地区省 → 城市逐级选择；没有城市数据时只显示省级。
- 教材卡纵向排列；版本选择用 BottomSheet 或全屏列表。
- 底部主 CTA 避开 `safe-area-inset-bottom`，可单手操作。

## 12. 家长辅助

教材确认页可以显示“请家长帮我确认”入口。MVP 只设计入口、说明和返回行为，不实现家长账号、短信、扫码或账号联动。家长看到的内容必须与数据层解析结果一致。

## 13. Onboarding 验收

1. 首次用户能理解地区 → 年级 → 教材 → 角色的顺序。
2. 地区页面不把出版社作为地区选项，也不滥用传统 Cascader。
3. 年级卡能区分“当前支持”和“即将开放”，未实现年级不能进入空页。
4. 三科教材卡可以分别确认，多个版本能手动选择。
5. 无默认、无候选、未支持地区和解析失败都有可恢复状态。
6. 选择结果形成三科独立的 `StudentCurriculumProfile`，不是单一教材字段。
7. 返回、刷新、离线和保存失败不会丢失草稿或静默覆盖旧档案。
8. Mobile 可以单手完成教材选择，Desktop / Tablet 不把表单简单缩小。
9. 页面只展示数据层提供的教材信息，不生成真实地区教材映射。
10. 完成后进入 Home，再次打开不重复 Onboarding。

